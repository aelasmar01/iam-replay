# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
# Saved plan files: binary, and they embed the account ID and resource names
tfplan
*.tfplan
*.tfvars
!example.tfvars
crash.log

# Local trail data pulled with `aws s3 sync` — never commit real logs
trail-data/

# OS
.DS_Store

# Rendered fixture policies carry a real account ID and bucket names
terraform/rendered/
